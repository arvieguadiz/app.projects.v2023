/*
 * Drawer Messages
 *
 * This contains all the text for the Drawer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Drawer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Drawer container!',
  },
});
